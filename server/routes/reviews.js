const express = require('express');
const router = express.Router();

// Routes for reviews are now handled in movies.js under /api/movies/:id/reviews
// We can keep this file if we want to add general review management later (e.g. admin delete review)

module.exports = router;
